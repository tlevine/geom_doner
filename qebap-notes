ggplot(df) +
  aes(label = paste0('Make #', id, ' (', manufacturer, ' ', model, ')'),


      kajmak = c(TRUE, FALSE),
      shape = c('cylindar', 'ball'),
      spicy = c(TRUE, FALSE),      
      count = 1:10, # How many pieces?


      border = drv,
      knoblauch = truck,
      scharf = grepl('auto', trans),
      zwiebeln = y2008,
      tomaten = TRUE, salat = TRUE,
      x = hwy, y = cty) +
  geom_qebap()
